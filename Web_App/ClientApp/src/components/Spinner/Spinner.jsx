import React, { useState, useEffect } from 'react';
import { RingLoader } from 'react-spinners';

const Spinner = () => {

    return <RingLoader color={'#000000'} size={200} />
}

export default Spinner;